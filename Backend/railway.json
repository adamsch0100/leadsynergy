{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install uv && uv sync --no-dev && uv run playwright install chromium && uv run playwright install-deps chromium"
  },
  "deploy": {
    "startCommand": "uv run python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
